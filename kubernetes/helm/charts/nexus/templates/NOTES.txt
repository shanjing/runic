Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
  $ helm get all {{ .Release.Name }} -n {{ .Release.Namespace }}

The Nexus (Coordination Service) has been deployed with the following configuration:

- Namespace: {{ include "scs.namespace" . }}
- Replicas: {{ .Values.replicaCount }}
- Service Account: {{ include "scs.serviceAccountName" . }}

{{- if .Values.ingress.enabled }}

Ingress is enabled. Your application will be available at:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- end }}

To check the status of the StatefulSet:

  $ kubectl get statefulset {{ include "scs.fullname" . }} -n {{ include "scs.namespace" . }}

To check the status of the pods:

  $ kubectl get pods -n {{ include "scs.namespace" . }} -l app.kubernetes.io/name={{ include "scs.name" . }}

To view the logs:

  $ kubectl logs -f statefulset/{{ include "scs.fullname" . }} -n {{ include "scs.namespace" . }}

To port-forward the API service locally:

  $ kubectl port-forward svc/{{ include "scs.fullname" . }}-api {{ .Values.service.api.port }}:{{ .Values.service.api.port }} -n {{ include "scs.namespace" . }}

Then access the API at: http://localhost:{{ .Values.service.api.port }}

To check the health of the service:

  $ curl http://localhost:{{ .Values.service.api.port }}/health/ready

{{- if .Values.serviceMonitor.enabled }}

Prometheus ServiceMonitor is enabled. Metrics are available at:
  http://{{ include "scs.fullname" . }}-api:{{ .Values.service.metrics.port }}/metrics
{{- end }}

{{- if .Values.podDisruptionBudget.enabled }}

PodDisruptionBudget is configured with minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
This ensures high availability during node maintenance.
{{- end }}

For more information, visit:
  - Architecture: https://github.com/your-org/scs/blob/main/docs/scs-architecture.md
  - Documentation: https://github.com/your-org/scs

Happy coordinating! 🚀

